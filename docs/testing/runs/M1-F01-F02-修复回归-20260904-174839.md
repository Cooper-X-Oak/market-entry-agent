# M1 F01/F02 修复回归

## 结论

**PASS**。

F01 与 F02 已完成最小修复、同步到当前 Vultr 4 GB 测试服务器，并用新的可视 Agent Browser Session 重跑：

- ENV-01：PASS
- M1-SRC-01：PASS
- M1-SRC-02：PASS

本轮没有扩大到 M2/M3，也没有重跑上轮已经通过的 Auth、Workspace、Lifecycle、Transaction、Temporal、Projector、SSE 和 ZIP。

## 修复内容

修改文件：`apps/web/components/source-manager.tsx`

### F01

URL 与 PDF 表单在异步请求前保存稳定的 form 引用：

```tsx
const form = event.currentTarget;
const data = new FormData(form);
await apiClient(...);
form.reset();
router.refresh();
```

这样请求成功后可以正常清空表单并刷新 Server Component 数据，不再访问已经失效的 `event.currentTarget`。

### F02

Source 时间统一使用显式的 `Asia/Shanghai` 时区：

```tsx
new Intl.DateTimeFormat('zh-CN', {
  timeZone: 'Asia/Shanghai',
  year: 'numeric',
  month: 'numeric',
  day: 'numeric',
  hour: '2-digit',
  minute: '2-digit',
  second: '2-digit',
  hour12: false,
})
```

远端 SSR 与本地浏览器现在生成相同的时间文本。

## 部署身份

| 项目 | 值 |
|---|---|
| Run ID | `m1-source-fix-20260904-174839` |
| 时间 | 2026-09-04 17:48–17:59，Asia/Shanghai |
| 服务器 | Vultr 4 GB，Instance `6c5e4ed1-c515-400d-9ba8-ceb59cd5de8a` |
| 代码基线 | `main@6159dc7f1c37ac1065bfd74262d7ce59a568967f` + M1 working tree + F01/F02 fix |
| 本地文件 SHA256 | `74cf062947d8cdb181f701e07fc622cb20e18d2f17676f4ada751f6551661f8b` |
| 远端文件 SHA256 | `74cf062947d8cdb181f701e07fc622cb20e18d2f17676f4ada751f6551661f8b` |
| Canary Mission | `29acbfd2-d766-45d5-822a-821701c2b7ba` |
| Agent Browser Session | `m1-source-fix-20260904-174839` |

定向 eslint 与 TypeScript 均通过。

最初通过当前 Codex pnpm wrapper 启动检查时，pnpm 11.19.0 因现有 `node_modules` 的版本状态请求无 TTY purge，命令在执行代码检查前中止；没有变更依赖。随后直接使用仓库已有的 eslint/tsc 可执行文件执行相同定向检查并通过。该项属于本地检查工具环境，不是产品缺陷。

## 验收结果

### ENV-01 — PASS

在 URL 和 PDF 两个 Source 都存在后执行硬刷新：

```text
uploaded_document · 最近抓取 2026/9/4 17:58:30
search_result · 最近抓取 2026/9/4 17:57:54
```

结果：

```text
Agent Browser page errors: none
role=alert: 0
Next.js issues overlay: 0
```

页面 Accessibility Snapshot 中只有普通的 `Open Next.js Dev Tools`，没有 `Open issues overlay`。F02 已关闭。

### M1-SRC-01 — PASS

可视浏览器提交：

```text
https://example.org/
```

结果：

```text
POST /sources/url: HTTP 201
页面无需手动刷新立即显示：Example Domain
URL 输入框：空
错误提示：无
Source ID：42bfd34e-c6aa-4997-9d82-c93649ce63e0
Snapshot ID：b0b68590-614c-4d65-a34b-4248a1282324
```

### M1-SRC-02 — PASS

可视浏览器上传一页有效 PDF：

```text
m1-source-fix-20260904-174839.pdf
```

结果：

```text
POST /sources/upload: HTTP 201
页面无需手动刷新立即显示上传文件名
文件输入框：空
错误提示：无
Source ID：fbf22032-2917-4727-8bf3-9b548c219786
Snapshot ID：3e8a4849-1f88-46e3-bb79-06214fd3c764
```

## 完整录像

文件：`.grok/verify-artifacts/m1-source-fix-20260904-174839/m1-source-fix-full.webm`

```text
Codec: VP8
Resolution: 1558 × 912
Frame rate: 10 fps
Duration: 120.0 seconds
Size: 1310408 bytes
SHA256: E92B173392FB81833D2DCFC8857E1DD4F703FA63AFDBC9F5F161CA08B1ADD813
```

已实际抽帧查看完整覆盖：

```text
1 秒：新 Session 登录页
30 秒：登录并选择 Workspace 后的 Dashboard
60 秒：新 Canary Mission 的空 Source 页面
90 秒：URL Source 已立即出现
115 秒：URL 与 PDF Source 均已出现，表单已清空，无错误浮层
```

本次 `record stop` 正常成功，录像可解码，覆盖本轮完整操作。

## 证据

证据目录：`.grok/verify-artifacts/m1-source-fix-20260904-174839/`

- `inspection-run.json`：机器可读范围、证据和 Verdict。
- `runtime-evidence.txt`：部署哈希、浏览器、API、Source/Snapshot、录像与清理摘要。
- `screenshot-1788515894609.png`：URL Source 成功后立即显示。
- `screenshot-1788515939212.png`：PDF 和 URL Source 同时显示；表单清空；无错误浮层。
- `m1-source-fix-full.webm`：本轮 120 秒完整录像。
- `video-1sec.png`、`video-30sec.png`、`video-60sec.png`、`video-90sec.png`、`video-115sec.png`：录像覆盖抽样帧。

## 清理状态

- 可视 Agent Browser 已关闭，无活动 Session。
- 本地临时 PDF 已删除。
- Canary Mission、Source、Snapshot 保留在服务器作为本次验收证据。
- Vultr 4 GB 服务器保持运行；本轮未获得服务器生命周期操作授权。
- 未执行 Git commit。

## 当前判定

上轮 [M1 可视验收](./M1-可视验收-20260904-170929.md) 中的 F01、F02 已由本轮直接证据关闭。M1 的这三个受影响验收项可以恢复为 PASS；后续工作仍按独立授权进入其他测试或 M2，不由本轮自动扩大。
