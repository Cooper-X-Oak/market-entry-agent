export function Panel({ title, description, action, children, noPadding = false }: { title?: string; description?: string; action?: React.ReactNode; children: React.ReactNode; noPadding?: boolean }) { return <section className="panel">{title && <header className="panel-header"><div><h2 className="panel-title">{title}</h2>{description && <div className="panel-description">{description}</div>}</div>{action}</header>}<div className={noPadding ? undefined : 'panel-body'}>{children}</div></section>; }

export function MetricStrip({ metrics }: { metrics: Array<{ label: string; value: React.ReactNode; detail?: string }> }) { return <section className="metric-strip" aria-label="关键指标">{metrics.map((metric) => <div className="metric" key={metric.label}><div className="metric-label">{metric.label}</div><div className="metric-value">{metric.value}</div>{metric.detail && <div className="metric-detail">{metric.detail}</div>}</div>)}</section>; }
